# dotfiles
```bash
sudo pacman -S pamac-gtk
sudo pamac install alacritty xfce4-clipman-plugin gufw pavucontrol discord xfce4-power-manager btop brave-browser neovim lightdm-gtk-greeter-settings polybar zcroll xfce-polkit ttf-roboto-mono ttf-roboto-slab ttf-roboto tela-circle-icon-git sxhkd spotify spotify-adblock rofi spicetify-cli  scrot python-pynvim playerctl picom-jonaburg-git nerd-fonts-roboto-mono nordic-theme-git betterdiscordctl 
sudo chmod a+wr /opt/spotify
sudo chmod a+wr /opt/spotify/Apps -R
betterdiscordctl install
git clone 
cd ~/dotfiles/config
cp nord-darker.theme.css ~/.config/BetterDiscord/themes/
cp -r alacritty ~/.config/
cp -r bspwm ~/.config
cp -r sxhkd ~/.config
cp Spicetify 
spicetify backup apply
spicetify config extensions fluent.js
spicetify config current_theme Fluent color_scheme dark
spicetify config inject_css 1 replace_colors 1 overwrite_assets 1
spicetify apply
cp -r polybar ~/.config
```
